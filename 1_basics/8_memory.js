/**
 * 연산하는 CPU / Memory
 * Memory: 데이터 저장
 */

/**
 * 메모리 셀: 1byte의 크기를 가짐.
 * 메모리 주소: 메모리 셀의 주소. 16진수로 나타냄.
 */

/**
 * let calc = 2 + 5 를 입력하였다면,
 * 1) 메모리에서 2,5를 가져옴
 * 2) CPU에서 연산을 함
 * 3) 7이라는 값을 뱉어내고 메모리에 다시 저장함
 * 
 * 메모리는 모든 OS에서 실행하는 모든 프로그램이 공유해서 쓰는 공간
 * 마음대로 주소 접근하면 문제가 생김
 * 
 * calc라는 변수는 메모리 주소를 가르킨다
 * 그래서 변수를 사용함
 */
